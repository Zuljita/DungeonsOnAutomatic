# Circular Room Generator Plugin

Generates a dungeon composed entirely of circular rooms. Useful as a reference
for custom room generation algorithms.

## Usage

Copy the folder into your project's `plugins/` directory and run:

```bash
pnpm doa generate --plugin-path examples/plugins/circular-room-generator --rooms 5
```

The generator will produce the requested number of circular rooms.
